# Mobile App (Flutter)

This is a starter shell (UI + API wiring).
The **hand-tracking engine** is implemented later as a Flutter platform plugin:

- Android: MediaPipe Tasks + TFLite GPU delegate
- iOS: CoreML + Vision (or custom pipeline)

Run:
```bash
flutter pub get
flutter run
```
